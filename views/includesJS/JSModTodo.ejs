	window.document.querySelector('input[type=text]').select();
	window.document.querySelector('button#validate').addEventListener('click', function () {
	});
 
	window.document.querySelector('button#cancel').addEventListener('click', function () {
		window.location.href('/');
	});
